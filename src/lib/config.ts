/**
 * Where the "시작하기" CTAs send users — the product app (ai.wearless.kr).
 * Override per-environment with NEXT_PUBLIC_APP_URL.
 */
export const APP_URL =
  process.env.NEXT_PUBLIC_APP_URL ?? 'https://ai.wearless.kr';
